{
  "installCommand": "npm install --prefix fittrack",
  "buildCommand": "npm run build --prefix fittrack",
  "outputDirectory": "fittrack/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
